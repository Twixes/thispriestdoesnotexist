# Conditional main adaptation from retained EMA250 importance

Prepared only; no native model load or main-adaptation launch. This separate route preserves the frozen step500-only runner. It is an explicit exception for the authenticated checkpoint250 from `adam-native1024-output-rank1-fixed-offsets100-to500-v1`, not permission to consume an arbitrary failed or unreviewed probe.

The source planned500 iterations but was stopped after264 because all four raw250 samples regressed photographically. Completed checkpoint250 remains available with exact restoration evidence. The two native review records cover all four raw and EMA images; EMA250 remained structurally coherent/source-like, without demonstrated priest conversion. That reviewed EMA state may supply diagnostic importance while the rejected raw model is discarded as a training continuation. Main adaptation still resets the original G/D, factors, Adam, RNG and path target; it never resumes either raw250 or EMA250 weights.

The observed successful FI4 diagnostic at250 is computational evidence only. **Actual completed FI1000, numerical validity and disjoint-half/rank stability review, followed by an explicit quantile decision, remain required before root schedules native10.** This runner checks FI1000 authenticity and refuses absent/partial artifacts, but does not automatically interpret the descriptive stability report or schedule itself. No default synthetic score, implicit quantile, automatic restart or generic failed-run fallback exists.

After those conditions are met:

```sh
research/.venv/bin/python research/experiments/adam_native/main_adaptation_fixed_offsets250_10.py \
  --importance-run research/runs/CHOOSE-THE-ACTUAL-COMPLETED-RETAINED250-FI1000-RUN \
  --quantile 50 \
  --output research/runs/adam-native1024-retained250-fixed-offsets-main-adaptation10-q50-v1
```

The placeholder is intentional. Quantile50 remains a development hypothesis based on the source Babies recipe, not a priest-calibrated setting or quality acceptance.

The new gate retains the original complete-FI protocol/result/supervisor/marker/sample-plan/accumulator checks and `validate_input`'s complete checkpoint authentication. It additionally requires exact source-step250 and checkpoint path; a hard-pinned known source directory; exact checkpoint, marker, protocol, snapshot manifest, inventory and runtime hashes; exact termination and failed-supervisor hashes; exact two review hashes and every image hash cited by those reviews. The stop record must say visual-review stop after264, last complete checkpoint250 and planned500, with source supervisor incomplete from KeyboardInterrupt. Both raw rejection and source-like EMA evidence remain explicit. It rejects altered/missing evidence, another source, relabeling the run as completed500 or replacing the stop with an unrelated failure. Historical process-absence records identify the stop event; they do not prove current liveness, so root still checks for another heavy worker before launch.

Each new protocol records the retained250 source path/step, stopped264 state, incomplete overall source run, reviewed evidence hashes, rationale and methodological departure from a completed500 probe. It pins and archives its own source plus the unchanged original main runner. The entire `AdaptationEngine`, worker training/checkpoint/preview/fold body, losses, update schedule, guards and ordinary one-thread CPU setting are unchanged. This variant adds no CPU-thread setting to main adaptation. The previous README describes mechanics of the frozen500 route; this new README is the conditional250-route status.

Four bounded tests compare the complete engine/worker AST and all unchanged FI authentication after only the declared source-gate substitution; use explicitly synthetic opaque checkpoint/image bytes in temporary directories to exercise the new provenance gate; reject changed/missing review/checkpoint/image evidence and unknown sources; and reject completed500/generic-failure relabeling even when synthetic fixture hashes are recomputed. Synthetic fixtures are never valid model files, never passed to a model loader and never persisted as research FI results. The first test run exposed a macOS temporary-path alias in the test fixture (`/var` versus `/private/var`); resolving the fixture root fixed the harness. No runtime training code was changed to accommodate that test issue.

The same35% start/20% runtime available-memory,12GiB RSS,512MiB swap-growth and1200second guards apply. Use one owned heavy worker. Native10 still must prove its new original-weight Adam memory use, exact source output, masks, checkpoint restoration and raw/EMA folding. Successful mechanics or coherent source EMA would not satisfy adult male priest appearance, photographic quality, variety or real-server generation below500ms.
