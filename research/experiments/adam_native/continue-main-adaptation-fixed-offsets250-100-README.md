# Conditional retained250 main-adaptation continuation:10→100

Prepared only. No native model was loaded and no continuation was launched. This is conditional on actual retained250 FI1000 with numerical/disjoint-rank stability review, an explicit quantile decision, and a successful **and visually reviewed native10 main-adaptation run**. Neither native10 completion nor its future checkpoint hash is invented here. Root schedules the continuation only if those conditions and the current machine state permit it.

```sh
research/.venv/bin/python research/experiments/adam_native/continue_main_adaptation_fixed_offsets250_100.py \
  --run research/runs/CHOOSE-THE-ACTUAL-SUCCESSFUL-RETAINED250-MAIN10-RUN \
  --output research/runs/adam-native1024-retained250-main10-to100-v1
```

The future source directory must contain:

- `protocol.json`, `result.json` and `supervisor.json`, declaring exactly10 completed main-adaptation iterations,13 G/11 D optimizer updates, successful raw/EMA folding and mask/fixed-offset checks, with no supervisor failure.
- `checkpoint-010.pt` and its complete `checkpoint-010.json` marker with actual checkpoint bytes SHA, full nested-state digest and source protocol identity.
- `runtime.json`, `modulation-inventory.json`, `trainable-inventory.json`, `initial-equality.json` and `eval-z.npz`.
- Both `step-000/` and `step-010/` manifests and all four raw/four EMA PNGs per step, with matching file hashes and unchanged-RNG assertions.
- Its authenticated FI1000 and specifically retained250 source lineage, including the stop/review evidence already required by the frozen native10 runner.

`validate_parent` checks those actual files, their hashes, success/count semantics, fixed parent-runner source hash and all source protocol pins. It reuses the retained250 FI gate. The existing numerical/disjoint stability review and native10 visual review are root scheduling requirements; this script does not automatically interpret qualitative review text or start itself.

The frozen parent marker contains `complete`, `step`, `checkpoint_sha256`, `state_sha256`, `protocol_sha256` and `model_optimizer_rng_path_state_restored_exactly`. It has no checkpoint-filename, optimizer-count or snapshot-manifest fields. The continuation therefore authenticates the fixed `checkpoint-010.pt` filename, reads counts from the result and restored engine, validates manifests separately, and reproduces every parent10 PNG before updating. Future50/100 markers written by this continuation explicitly include the additional filename/count/snapshot links.

The worker reconstructs original source G/D and installs the identical modulation inventory solely to establish authenticated original protected references and matching controllers. It creates the same `AdaptationEngine` class by import from the frozen retained250 native10 runner. Then it restores **G, D, G EMA, both Adam states, global Torch RNG, data-sampling RNG, path target, selections, controller flags, optimizer parameter order and absolute iteration counts** from the source10 checkpoint. Empty/fresh initialization never enters a continuation update. The entire restored engine digest must match the parent; parent runtime and modulation inventory must match exactly.

Before global iteration10 executes, every parent step10 raw/EMA PNG is rendered again and must match its archived bytes exactly. Previewing must leave both RNG streams unchanged. A mismatch aborts instead of rebaselining, reseeding, resetting Adam or falling back to fresh training.

The loop executes absolute indices10–99, adding90 iterations. Its first new completed step11 has no lazy regularizer; G path regularization next runs at index12, and both D R1/G path run at index16. The imported engine preserves all calculations, style mixing, sampling, horizontal flips, offsets, masks, learning rates, G EMA and one-thread native1024 FP32 behavior. No CPU-thread experiment is folded into this training continuation.

| Completed iterations | G optimizer updates | D optimizer updates |
|---:|---:|---:|
|10 source|13|11|
|50 checkpoint|63|54|
|100 checkpoint|125|107|

The continuation adds112 G and96 D optimizer updates. All four raw/four EMA fixed latents are saved at10,50 and100. Both50/100 checkpoints atomically retain complete engine state, child protocol/runtime/inventory hashes and source10 checkpoint identity, then independently reload and perform exact perturb/restore checks. Their markers also bind snapshot manifests and update counts. Checkpoint50 is a non-blocking native visual-review opportunity while root monitors the worker; it is not an automatic quality pass. Root can stop the owned process if images regress. At100 both raw and EMA folds must match all four native outputs exactly; exports are never deployed automatically.

Memory guards remain35% available at start,20% at runtime,12GiB owned-process-tree RSS and512MiB swap growth. The explicitly approved wall deadline is3600seconds for the90 additional updates. The supervisor owns and terminates only its subprocess group on failure. There is no automatic restart, deadline extension, concurrent native worker, modified objective or reduced-resolution fallback. Training another90 steps may still exceed the guards; the successful10-step run must provide the first relevant memory/timing evidence.

Three tiny-network tests use the actual imported frozen engine and the actual envelope-restoration helper. They prove complete uninterrupted10→100 state equality against a serialized/reconstructed run restored at10 and again50; compare every recorded loss/sample/regularizer event; exercise both regularizers at16/32/48/64/80/96; detect a reset-path-target negative control; and reject changed envelope/mask data before mutation. The test fixture uses tiny random native32 networks only. It loads no pretrained model, real FI accumulator or purported native checkpoint. Native image reproduction, native resource use and actual future source10 authentication still require a real conditional run.

This is a development continuation, not proof of convergence, photographic quality, adult male priest coverage, no hats, independent identities or real-server generation below500ms. Those remain necessary for the complete user goal.
