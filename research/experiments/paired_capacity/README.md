# Unused capacity draft

The parent task changed direction before execution or testing. This b32-unfreeze runner is retained as an unreviewed, untested historical draft. Its executable entrypoint is disabled. No real weights were loaded and no capacity training was performed. The current matched-objective proposal lives in `../paired_surround/`.
