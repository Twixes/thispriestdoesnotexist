# Fresh32 native NADA500 evaluation

Prepared by root; no model executed during preparation. All32 Gaussian z rows are fixed by PCG64 seed202609210900, disjoint the540 prior source/train/evaluation rows. The sampler requires actual successful500 completion and checkpoint hashes, reproduces fixed native RGB/grayscale preview000 exactly, then preserves all32 new full1024 RGB/grayscale samples without filtering or restoration.

CPU1, no competing model job at launch, fresh memory >=35%,6GiB sampled RSS and five-minute owned-child deadline. Final worker peak/deadline checks and unconditional owned-group cleanup were added after independent agent review. These are bounded research safeguards, not an OS memory allocation limit.

Run only after the current trainer is terminal:

    research/selection/.venv/bin/python research/experiments/nada_unseen32/sample.py --execute

Output: research/runs/nada-clean24-unseen32-500 (must not exist). Successful supervisor and evaluation records are required. Every output remains a research candidate; byte differences do not establish identity novelty or photographic quality.
