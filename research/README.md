# Priest generator research

Goal: train a real generative model and run fresh server-side inference on every page load. Sampling the static gallery, pixel morphing, or adding noise does not meet this goal. The production site remains on its curated static gallery until a model passes evaluation.

## Current status (2026-09-21)

The [latest matched review](reviews/sdxl-pilot-preview/index.html) now contains actual priest-adapted SDXL-Turbo outputs. Local20- and100-update rank16 LoRA runs completed in42.9s and173.0s, with finite updates and unchanged frozen base weights. All144 unique native development outputs are retained. Training is feasible locally, but this ordinary-denoising recipe did not improve one-step quality: same-trigger joint agent-review passes fell from12/24 at baseline/20 updates to9/24 at100. Cloudier eyes and more airbrushed textures trigger the declared stop before250. No sealed test or production deployment follows a failed gate. The initial four-step fallback attempt stopped at the unchanged memory reserve before producing images. Full provenance and failure evidence are in [diffusion research](diffusion/README.md).

The new [hosting comparison](reviews/gpu-hosting-options-2026-09-21.md) separates an inexpensive short GPU benchmark from24/7 hosting: no verified resident option fits the ideal$15/month budget, and serverless has cold starts. No GPU was rented. A [specialist photographic SDXL comparison](reviews/base-selection/photoreal-fast-sdxl.md) is the next alternative to extending a weak generic prior.

The [four-step fallback](reviews/sdxl-pilot-4step-preview/index.html) subsequently completed by caching conditioning and releasing the text encoders before image-model loading, with the same memory limits. It adds144 native outputs with numerically matched conditioning across the base and both adapters. Four steps improve realism substantially, but neither adapter passes the frozen quality gate. No test seeds were opened. The pinned Juggernaut X Hyper checkpoint and an offline evaluator are ready for the next photographic-base comparison; that model has not yet generated an evaluation image here.

The latest user requirements allow GPU hosting and require actual server generation below500ms. A [new pretrained-base comparison](diffusion/README.md) evaluated four diffusion bases in eight configurations, retaining all64 portraits. SDXL-Turbo512 leads the tested speed/appearance comparison: coherent initial priest portraits and430ms median warm generation plus WebP on this Mac, versus222ms for the smaller SD-Turbo. These are eight-sample local measurements, not server proof or priest post-training. Lightning portraits looked rendered, and Turbo at1024 produced severe facial duplication. FLUX.2-klein4B produced coherent priest portraits at512/1024 but conspicuous within-prompt face resemblance and formal staff-portrait styling; cached-prompt local medians were3.59s/10.51s. Its author-endorsed base-to-distilled LoRA route is the clearest documented adaptation path and was tested through local setup, but its first forward pass crossed the system-memory guard; no optimizer updates completed. The staged20-image/text cache succeeded, and all attempts are retained in the [smoke report](diffusion/flux-adaptation-smoke-report.json). A [non-blocking comparison preview](reviews/base-comparison-preview/index.html) shows every output. Official model weights, source versions, upstream licenses, exact noise and failed attempts are retained. The separate StyleGAN3 CPU probe exceeded its12GiB memory cap after a single warmup; it did not complete the comparison.

The [held-out restoration-selector report](reviews/reference625-test64/README.md) confirms the previous route failed:9/64 automatic accepts,2 of9 with visible eye defects, and recurring facial resemblance. Its threshold and labels remain frozen. Neither this selector nor any new pretrained base has been approved or deployed. The original goal still requires actual post-training, broad fresh-output quality/diversity and target-server latency verification.

No model has passed the visual quality requirement or been deployed. The guarded reference continuation completed step625 / 40,000 presentations with unchanged110-image inputs and recipe. All989 saved tensors were finite and144 EMA export tensors matched; the immutable checkpoint is preserved in [the run](runs/reference256-paper-b64-resumed500-to625/). Numeric integrity did not imply visual success: both fixed-grid architectural heads persist, and the [matched32 comparison](reviews/reference625-comparison/root-review-000625.json) shows continued face/background corruption and hat-like outputs. This adversarial run remains stopped at625 while the native-resolution alternative is assessed.

The isolated native1024 [NADA-style CPU smoke](runs/nada-clean24-cpu-smoke10/) completed ten updates in 77.5 seconds with 6.61 GiB peak worker RSS, exact reproduction of all 32 source images, finite gradients, unchanged frozen state and full optimizer/RNG restoration. The subsequent [checkpoint10→500 continuation](runs/nada-clean24-cpu-continue500/) completed 490 further updates in 22.66 minutes: mean 2.66 seconds per update including 0.60 seconds for its fresh source feature, with 6.53 GiB peak worker RSS. It preserved the original 24 targets and matched eight evaluation latents; initial step10 images reproduced exactly, and full checkpoints at 50/100/250/500 passed state, snapshot and reload checks. These are successful research computations, not visual approval.

The completed [unfiltered fresh32 evaluation](runs/nada-clean24-unseen32-500/evaluation.json) used 32 Gaussian latents disjoint from all 540 earlier source/training/evaluation rows, at psi1 with constant noise. Both [independent native reviews](runs/nada-clean24-unseen32-500/review-000-015.json) ([second half](runs/nada-clean24-unseen32-500/review-016-031.json)) found broad white clerical-style neck bands in all 32, but no convincing small Roman tabs. Together they counted 26 apparently adult faces, three childlike and three age-uncertain; four hats, two definite extra partial heads and one uncertain background-person silhouette remain. All 32 have native skin/hair/eye detail concerns, despite several promising adult portraits. This is meaningful learned clerical appearance with unresolved anatomy/detail, garment, age and headwear requirements—not a production-ready generator. Candidate subsets for further diagnostics do not change the full32 denominator.

A matched [text-direction probe](experiments/nada_text90/README.md) also completed 90 updates from the same checkpoint10 to total100, changing only the guidance vector and declared horizon. Its separate [run](runs/nada-text90-cpu-matched100/) is not combined with the image-guided result. The [objective audit](reviews/nada-directional-objective-audit.md) distinguishes implementation correctness from insufficiently specific CLIP supervision. The completed [GFPGAN diagnostic](restoration/README.md) restored all32 reference625 and all8 NADA500 samples. Intact faces became substantially more photographic; architecture/head mergers, hats and unsuitable age appearance remained. Native reviewers found15/32 restored reference portraits promising as whole images, versus no readiness for the unfiltered stream. The next [development128 cohort](restoration/runs/reference625-development128-v1/) contains128 new raw-G latents (14.43seconds CPU sampling);125 restored successfully and3 failed face detection over313.05seconds, with4.06GiB peak RSS. Its images are for training a lightweight quality selector, with disjoint future calibration and test seeds declared in [the protocol](experiments/restored_selection/protocol.json). They are never a serving seed/image bank. Repeated facial resemblance and whole-pipeline resource costs remain to be evaluated. Its upstream source, weights and license notices remain separate from the site's MIT code.

An earlier [overlap attempt](runs/nada-clean24-overlap-cpu-smoke10/failure-review.json) stopped because a metadata-only validation check tripped its conservative competing-process guard; it provides no model-training result.

A newer [36-image whole-portrait dataset](data/plain-background36-whole-image/README.md) is available for future experiments. It retains24 previously selected originals and adds12 from16 newly generated/reviewed portraits. Both NADA training experiments used the original24, unchanged.
The separate native 1024px [paired factorial](runs/paired-factorial-600/) completed all four variants, each with 300 updates and matched sampling. The [final root review](reviews/factorial-ABCD300-audit/root-final-decision.json) rejects all four: skin-like neck bands and poor held-out collar generalization persist. This paired-objective experiment will not be extended; lower pixel errors did not establish quality.

A prospective [134-image dataset](data/priests134-expanded256/README.md) combines the original 110 byte-for-byte with 24 newly reviewed [whole-image portraits](data/plain-background24-whole-image/README.md). These are synthetic training inputs generated with imagegen, not output from our trained GAN. Originals, prompts, unsuccessful alignment attempts and all derivatives are preserved. The new set changes framing and dataset identity; using it requires a declared experiment fork. It is not substituted into the exact 500-to-625 continuation.

The latest measured full-reference throughput extrapolates to 5.19 days per million presentations on this Mac, excluding checkpoint/preview overhead. This is a limited timing observation, not a quality forecast. The [resource comparison](reviews/stylegan-resource-reference.md) distinguishes it from earlier portable-loop timings and NVIDIA's GPU benchmarks.

## Earlier experiments and reference implementation

Transfer learning from NVIDIA's FFHQ StyleGAN2 at 256px. The initial 50-image pilot was deliberately stopped after its durable step-1000 checkpoint: collars appeared, but fixed samples showed overlapping facial features and conspicuously similar faces/compositions. Its source, weights, logs, and rejected visual reviews remain in `runs/pilot256/`.

The now-paused restart, `aligned110-frozen4`, uses those 50 selected portraits plus 60 newly generated and visually reviewed fictional adult portraits. Original generations, prompts, reviews, and hashes are archived in `data/synthetic/`. The training inputs are `alignment/collar-only/eyes42/`: 110 separate images with a collar-preserving crop that reduces the measured framing mismatch with FFHQ. No hats were introduced. Full FFHQ cropping was rejected because it removed most collars; the earlier whole-head constraint was also relaxed because it unnecessarily limited face size. See `alignment/README.md` and `alignment/collar-only/README.md` for evidence and exact transforms.

The reference network code is reused without modifications. The earlier `train.py` provides a portable PyTorch training loop with non-saturating logistic losses, lazy R1, style mixing, EMA, and MIT Han Lab's differentiable color/translation/cutout augmentation. Augmentation probability follows the ADA discriminator-sign heuristic. That earlier loop disables path-length regularization and differs from NVIDIA's full augmentation pipeline; the later reference-phase trainer below includes both.

The paused control freezes the first four discriminator layers using NVIDIA's built-in FreezeD buffers, uses mapping LR .0005 and EMA half-life .5 kimg, and originally allowed up to 6000 batch-8 steps on the local Apple GPU. It was intentionally paused at its verified, durable step-1000 checkpoint to make room for the reference baseline; this is not a claim that it had reached final convergence. See its `pause.json` for the exact checkpoint hash and continuation command. Frozen weights, optimizer restoration, R1, and side-effect-free snapshots passed real CPU smoke checks. Every 250 steps it saves matched raw-G and EMA grids at truncation 1.0, plus the usual EMA grid at .7. Review these before deciding to continue; a long run is not itself evidence of acceptable quality. This combines several evidence-driven changes and is not an isolated causal ablation. See `reviews/few-shot-options.md`.

The 6000-step limit is only 48,000 image presentations and is a feasibility budget. NVIDIA reports around one million presentations as a common transfer-learning reference, roughly 3.5 days at the measured speed of the earlier portable loop at 256px. This estimate does not apply to the slower full reference loop with both regularizers and is not a quality guarantee. The original 1024px StyleGAN used 70,000 photographs and about a week on eight V100 GPUs; our pretrained weights reuse that kind of upstream investment. See [resource comparison](reviews/stylegan-resource-reference.md) for primary sources and the distinction between unique images, presentations, and optimizer steps.

A paired fresh-base run, `aligned110-frozen4-cdc1000`, used the same dataset, seed, FreezeD and adversarial settings, with an additional source-correspondence loss at weight 1000. It was deliberately stopped after its preserved step-4750 checkpoint, with 38,000 presentations and persistent facial defects. Its independent latent stream kept the adversarial RNG unaffected. Real MPS G/D/R1 updates and a full checkpoint/optimizer/RNG round trip passed. See [CDC experiment](experiments/cdc/README.md). Both variants remain unapproved. The control step-1000 review showed clearer features but strongly contracted identity diversity. Earlier CDC/reference overlap means those wall times were not isolated measurements.

The `reference256-paper-b64` baseline reuses NVIDIA's unmodified loss and augmentation code with separate main/regularization Adam phases, PL and R1, transfer ADA, 20-kimg EMA, batch 64/microbatch 8, source minibatch-statistics group 8, and mirrored data (110 originals, 220 virtual entries). Its original horizon was 15,625 total updates, or one million presentations, but it was deliberately paused after preserving step 500. Checkpoints/previews occur every 125 updates (8 kimg). CPU exact continuation, snapshot side effects, actual intended MPS update, and safe generator export were tested; these checks do not approve visual quality. See [trainer notes](experiments/reference_phases/trainer-notes.md), [batch64 evidence](experiments/reference_phases/b64-m8-report.md), and `runs/reference256-paper-b64/experiment.json`. Compare experiments at equal image presentations, not equal optimizer steps. The independent [trainer audit](reviews/reference-trainer-audit.md) found no invalidating math error. Future resumes must use the separate [resume preflight](experiments/reference_phases/resume-preflight.md), because the recipe gate alone does not enforce trainer, adapter, network-operation and runtime provenance.

Twelve fresh [held-out portraits](data/validation12/README.md) remain excluded from training. The old control scores them lower than training inputs in both matched discriminator contexts, including a lower-padding subset. This is a diagnostic signal, not proof of memorization: rendering, pose, selection and alignment differences remain confounds. See [held-out diagnostic](reviews/heldout-discriminator/summary.md).

## Reproduce the earlier portable-loop experiment

```sh
uv venv --python 3.11 research/.venv
uv pip install --python research/.venv/bin/python -r research/requirements-lock.txt
git lfs pull --include="research/**" --exclude=""
research/.venv/bin/python research/prepare_data.py --synthetic --output research/data/hot110-256
research/.venv/bin/python research/benchmark.py --device cpu
research/.venv/bin/python research/benchmark.py --device mps --training
research/.venv/bin/python -u research/train.py --device mps --data research/alignment/collar-only/eyes42 --freeze-d-layers 4 --run aligned110-frozen4-new-reproduction --steps 6000 --batch 8 --snapshot-every 250
```

These commands describe the earlier portable loop, not the full reference trainer. Choose an unused run name and preserve archived outputs. To deliberately resume that earlier control, use its preserved checkpoint with the same `--freeze-d-layers 4`; a mismatch is rejected rather than silently corrupting discriminator optimizer state. For the current reference continuation, use its separately reviewed launcher and provenance instructions linked above. Reproduce alignment inputs with the separate instructions in `alignment/`; they are also stored directly in Git LFS. The initial pretrained pickle is trusted NVIDIA code, fetched only from the URL and SHA256 in `sources.json`. Do not load arbitrary pickles.

## Measured feasibility

On the owner's Apple M5 Pro (48 GB), warm 256px inference measured 0.128–0.165 seconds on CPU (8 threads), and 0.023 seconds on MPS. Single-image training forward/backward and R1 second derivatives succeeded on MPS. Raw timings and environment versions are in `runs/benchmark-*.json`. These do not establish latency on a smaller rented CPU or validate priest image quality.

The first benchmark attempt hit a PyTorch optimizer parameter-type error before training; its log is retained and the float parameter was corrected. A successful rerun is recorded separately.

The verified official FFHQ 1024px checkpoint is now archived with provenance in `models/ffhq1024.pkl`. A research-only generator export passed the actual CPU serving path: warm median 0.996 seconds including WebP encoding and process peak RSS 3.42 GiB on this Mac. This exceeds the inactive Cloudflare candidate's 3 GiB setting. A subsequent Linux/amd64 Docker test with a 4 GiB memory limit and one CPU completed four 1024px generations with a 1.50 GiB cgroup peak and no OOM. This demonstrates that native Mac memory usage is not a Linux sizing prediction; final-model HTTP/concurrency and actual hosting checks remain. See [Linux container evidence](runs/inference-cpu/ffhq1024/docker-4g/README.md). Native Mac or emulation timing is not a production-host guarantee. These baseline faces are not a trained priest model and the production gate rejects this unreviewed export. See [1024 benchmark](runs/inference-cpu/ffhq1024/README.md).

## Evaluation required before production

- Inspect fixed and unseen random latent grids: realistic adult male faces, clearly visible clerical collar, no hats, calendar-style appearance, no frequent artifacts.
- Check diversity and training-set nearest neighbors; a distinct byte hash alone is not evidence of a distinct identity.
- Compare fixed latent samples over checkpoints for mode collapse and overfitting.
- Benchmark the actual inference container, cold start and warm request latency.
- Exercise concurrent server requests, fresh randomness, image dimensions, no-store caching, and failure responses.
- Deploy through CI from `master`, then verify the public domain makes real inference requests and produces different decoded images.

## Hosting research

Ordinary Cloudflare Workers have 128 MB memory and are unsuitable for this PyTorch model. Cloudflare Containers can run CPU PyTorch behind the existing Worker and keep everything in the personal account. They require Workers Paid ($5/month) plus metered container usage. The owner prefers local training and hosting below $15/month. No paid upgrade or rented GPU has been purchased. A fixed-price personal CPU server behind the Cloudflare frontend is also being evaluated to keep the bill predictable.

References checked 2026-09-20:
- https://developers.cloudflare.com/workers/platform/limits/
- https://developers.cloudflare.com/containers/platform/pricing/
- https://developers.cloudflare.com/containers/examples/static-frontend-container-backend/
- https://developers.cloudflare.com/workers/ci-cd/builds/build-image/

The repo default branch and CI production branch have been changed to `master` per the new instruction. Git LFS stores all raster images and models. CI explicitly hydrates `public/**` before validating and deploying, avoiding image-pointer files being published.

Default clones hydrate only `public/**` through `.lfsconfig`, so frontend CI does not repeatedly download experimental checkpoints. Research reproduction explicitly fetches `research/**`; inference CI explicitly fetches only `models/production/**`. All artifacts remain versioned in Git LFS.
